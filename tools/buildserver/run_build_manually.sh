#!/bin/bash

# Useful options:
# Don't send email: -n
#$HOME/autobuild/autobuild -u -l -b $HOME/autobuild $HOME/autobuild/autobuild-c3po-root.conf
$HOME/autobuild/autobuild -u -l -b $HOME/autobuild $HOME/autobuild/autobuild-c3po-root.conf
