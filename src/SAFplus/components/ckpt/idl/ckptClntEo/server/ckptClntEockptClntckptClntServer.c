/*********************************************************************
* ModuleName  : idl
*********************************************************************/
/*********************************************************************
* Description :Server Stub routines
*     
* THIS FILE IS AUTO-GENERATED BY OPENCLOVIS IDE. EDIT THIS FILE AT
* YOUR OWN RISK. ANY CHANGE TO THIS FILE WILL BE OVERWRITTEN ON
* RE-GENERATION.
*     
*********************************************************************/
#include <netinet/in.h>
#include <clBufferApi.h>
#include <clRmdApi.h>
#include <clEoApi.h>
#include <ipi/clRmdIpi.h>
#include <string.h>
#include "ckptClntEockptClntckptClntServer.h"
#include "ckptClntEoServer.h"

extern ClUint32T  ckptClntEoidlSyncKey;
extern ClHandleDatabaseHandleT  ckptClntEoidlDatabaseHdl;



ClRcT clCkptSectionUpdationNotificationServer_4_0_0(ClEoDataT eoData, ClBufferHandleT inMsgHdl, ClBufferHandleT outMsgHdl)
{
    ClIdlContextInfoT *pIdlCtxInfo = NULL;
    ClRcT rc = CL_OK;
    ClNameT  pName;
    ClCkptSectionIdT_4_0_0  pSecId;
    ClUint32T  dataSize;
    ClUint8T*  pData;

    memset(&(pName), 0, sizeof(ClNameT));
    memset(&(pSecId), 0, sizeof(ClCkptSectionIdT_4_0_0));
    memset(&(dataSize), 0, sizeof(ClUint32T));
    memset(&(pData), 0, sizeof(ClUint8T*));


    rc = clXdrUnmarshallClNameT( inMsgHdl,&(pName));
    if (CL_OK != rc)
    {
        goto LL0;
    }

    rc = clXdrUnmarshallClCkptSectionIdT_4_0_0( inMsgHdl,&(pSecId));
    if (CL_OK != rc)
    {
        goto LL1;
    }

    rc = clXdrUnmarshallClUint32T( inMsgHdl,&(dataSize));
    if (CL_OK != rc)
    {
        goto LL2;
    }

    rc = clXdrUnmarshallPtrClUint8T( inMsgHdl,(void**)&(pData), dataSize);
    if (CL_OK != rc)
    {
        goto LL3;
    }

    pIdlCtxInfo = (ClIdlContextInfoT *)clHeapAllocate(sizeof(ClIdlContextInfoT));
    if(pIdlCtxInfo == NULL)
    {
       return CL_IDL_RC(CL_ERR_NO_MEMORY);
    }
    memset(pIdlCtxInfo, 0, sizeof(ClIdlContextInfoT));
    pIdlCtxInfo->idlDeferMsg = outMsgHdl; 
    pIdlCtxInfo->inProgress  = CL_FALSE;
    rc = clIdlSyncPrivateInfoSet(ckptClntEoidlSyncKey, (void *)pIdlCtxInfo);
    if (CL_OK != rc)
    {
        clHeapFree(pIdlCtxInfo);
        goto L0;
    }
    rc = clCkptSectionUpdationNotification_4_0_0(&(pName), &(pSecId), dataSize, pData);
    if(pIdlCtxInfo->inProgress == CL_FALSE)
    {
      clHeapFree(pIdlCtxInfo);
      pIdlCtxInfo = NULL;
    }
    if (CL_OK != rc)
    {
       goto L0;
    }
    
    rc = clXdrMarshallClNameT(&(pName), 0, 1);
    if (CL_OK != rc)
    {
        goto L1;
    }

    rc = clXdrMarshallClCkptSectionIdT_4_0_0(&(pSecId), 0, 1);
    if (CL_OK != rc)
    {
        goto L2;
    }

    rc = clXdrMarshallClUint32T(&(dataSize), 0, 1);
    if (CL_OK != rc)
    {
        goto L3;
    }

    rc = clXdrMarshallPtrClUint8T(pData, dataSize, 0, 1);
    if (CL_OK != rc)
    {
        goto L4;
    }

    if(pIdlCtxInfo != NULL)
    {
      clHeapFree(pIdlCtxInfo);
      return rc;
    }
    
L4:    return rc;

LL3:  clXdrMarshallPtrClUint8T(pData, dataSize, 0, 1);
LL2:  clXdrMarshallClUint32T(&(dataSize), 0, 1);
LL1:  clXdrMarshallClCkptSectionIdT_4_0_0(&(pSecId), 0, 1);
LL0:  clXdrMarshallClNameT(&(pName), 0, 1);

    return rc;

L0:  clXdrMarshallClNameT(&(pName), 0, 1);
L1:  clXdrMarshallClCkptSectionIdT_4_0_0(&(pSecId), 0, 1);
L2:  clXdrMarshallClUint32T(&(dataSize), 0, 1);
L3:  clXdrMarshallPtrClUint8T(pData, dataSize, 0, 1);


    return rc;
}

ClRcT clCkptSectionUpdationNotificationResponseSend_4_0_0(ClIdlHandleT idlHdl,ClRcT retCode)
{
    ClIdlSyncInfoT    *pIdlSyncDeferInfo = NULL;
    ClRcT              rc                = CL_OK;
    ClBufferHandleT outMsgHdl     = 0;
    
    rc = clHandleCheckout(ckptClntEoidlDatabaseHdl,idlHdl,(void **)&pIdlSyncDeferInfo);
    if( rc != CL_OK)
    {
      goto Label0; 
    }
    outMsgHdl = pIdlSyncDeferInfo->idlRmdDeferMsg;
    
    rc = clIdlSyncResponseSend(pIdlSyncDeferInfo->idlRmdDeferHdl,outMsgHdl,
                                retCode);
    

    

    clHandleCheckin(ckptClntEoidlDatabaseHdl, idlHdl);
    clHandleDestroy(ckptClntEoidlDatabaseHdl, idlHdl);
Label0:
    return rc;
}

ClRcT clCkptWriteUpdationNotificationServer_4_0_0(ClEoDataT eoData, ClBufferHandleT inMsgHdl, ClBufferHandleT outMsgHdl)
{
    ClIdlContextInfoT *pIdlCtxInfo = NULL;
    ClRcT rc = CL_OK;
    ClNameT  pName;
    ClUint32T  numberOfElements;
    ClCkptIOVectorElementT_4_0_0*  pIoVector;

    memset(&(pName), 0, sizeof(ClNameT));
    memset(&(numberOfElements), 0, sizeof(ClUint32T));
    memset(&(pIoVector), 0, sizeof(ClCkptIOVectorElementT_4_0_0*));


    rc = clXdrUnmarshallClNameT( inMsgHdl,&(pName));
    if (CL_OK != rc)
    {
        goto LL0;
    }

    rc = clXdrUnmarshallClUint32T( inMsgHdl,&(numberOfElements));
    if (CL_OK != rc)
    {
        goto LL1;
    }

    rc = clXdrUnmarshallPtrClCkptIOVectorElementT_4_0_0( inMsgHdl,(void**)&(pIoVector), numberOfElements);
    if (CL_OK != rc)
    {
        goto LL2;
    }

    pIdlCtxInfo = (ClIdlContextInfoT *)clHeapAllocate(sizeof(ClIdlContextInfoT));
    if(pIdlCtxInfo == NULL)
    {
       return CL_IDL_RC(CL_ERR_NO_MEMORY);
    }
    memset(pIdlCtxInfo, 0, sizeof(ClIdlContextInfoT));
    pIdlCtxInfo->idlDeferMsg = outMsgHdl; 
    pIdlCtxInfo->inProgress  = CL_FALSE;
    rc = clIdlSyncPrivateInfoSet(ckptClntEoidlSyncKey, (void *)pIdlCtxInfo);
    if (CL_OK != rc)
    {
        clHeapFree(pIdlCtxInfo);
        goto L0;
    }
    rc = clCkptWriteUpdationNotification_4_0_0(&(pName), numberOfElements, pIoVector);
    if(pIdlCtxInfo->inProgress == CL_FALSE)
    {
      clHeapFree(pIdlCtxInfo);
      pIdlCtxInfo = NULL;
    }
    if (CL_OK != rc)
    {
       goto L0;
    }
    
    rc = clXdrMarshallClNameT(&(pName), 0, 1);
    if (CL_OK != rc)
    {
        goto L1;
    }

    rc = clXdrMarshallClUint32T(&(numberOfElements), 0, 1);
    if (CL_OK != rc)
    {
        goto L2;
    }

    rc = clXdrMarshallPtrClCkptIOVectorElementT_4_0_0(pIoVector, numberOfElements, 0, 1);
    if (CL_OK != rc)
    {
        goto L3;
    }

    if(pIdlCtxInfo != NULL)
    {
      clHeapFree(pIdlCtxInfo);
      return rc;
    }
    
L3:    return rc;

LL2:  clXdrMarshallPtrClCkptIOVectorElementT_4_0_0(pIoVector, numberOfElements, 0, 1);
LL1:  clXdrMarshallClUint32T(&(numberOfElements), 0, 1);
LL0:  clXdrMarshallClNameT(&(pName), 0, 1);

    return rc;

L0:  clXdrMarshallClNameT(&(pName), 0, 1);
L1:  clXdrMarshallClUint32T(&(numberOfElements), 0, 1);
L2:  clXdrMarshallPtrClCkptIOVectorElementT_4_0_0(pIoVector, numberOfElements, 0, 1);


    return rc;
}

ClRcT clCkptWriteUpdationNotificationResponseSend_4_0_0(ClIdlHandleT idlHdl,ClRcT retCode)
{
    ClIdlSyncInfoT    *pIdlSyncDeferInfo = NULL;
    ClRcT              rc                = CL_OK;
    ClBufferHandleT outMsgHdl     = 0;
    
    rc = clHandleCheckout(ckptClntEoidlDatabaseHdl,idlHdl,(void **)&pIdlSyncDeferInfo);
    if( rc != CL_OK)
    {
      goto Label0; 
    }
    outMsgHdl = pIdlSyncDeferInfo->idlRmdDeferMsg;
    
    rc = clIdlSyncResponseSend(pIdlSyncDeferInfo->idlRmdDeferHdl,outMsgHdl,
                                retCode);
    

    

    clHandleCheckin(ckptClntEoidlDatabaseHdl, idlHdl);
    clHandleDestroy(ckptClntEoidlDatabaseHdl, idlHdl);
Label0:
    return rc;
}

