################################################################################
# 
# This Makefile builds the software in the defined subdirectories.
# It expects that the SAFplus build environment is defined
# by "sourcing" clasp.env in your model's build/local directory.
#
# This Makefile is auto-generated by OpenClovis IDE
#
################################################################################
include $(CLOVIS_ROOT)/SAFplus/mk/preface.mk

# Component name (using all lowercase):
EXENAME := ${safComponent_instantiateCommand}

# Some default meanings if not overridden by a calling makefile
MKDIR ?= mkdir
CP    ?= cp

# List local source files needed for the component server:

SRC_FILES := $(wildcard *.py)

PY_TGT := $(addprefix $(BIN_DIR)/,$(notdir $(SRC_FILES)))

all: $(PY_TGT) $(BIN_DIR)/$(EXENAME)

libs:  # Nothing to to for the libs stage...

$(BIN_DIR)/$(EXENAME): $(EXENAME)
	$(MKDIR) -p $(BIN_DIR)
	$(CP) $^ $@

$(BIN_DIR)/%.py: %.py
	$(MKDIR) -p $(BIN_DIR)
	$(CP) $< $@

clean: 
	rm -f $(BIN_DIR)/$(EXENAME) $(PY_TGT)
