/* 
 * File Level.cxx
 * This file has been auto-generated by Y2CPP, the
 * plug-in of pyang.
 */ 

#include "myServiceCommon.hxx"
#include <iostream>
#include "MgtEnumType.hxx"
#include "Level.hxx"

using namespace myService;

namespace myService
  {

    /*
     * Provide an implementation of the en2str_map lookup table.
     */
    const LevelManager::map_t LevelManager::en2str_map = {
            pair_t(Level::gold, "gold"),
            pair_t(Level::silver, "silver"),
            pair_t(Level::copper, "copper")
    }; // uses c++11 initializer lists 

    const char* c_str(const myService::Level &level)
    {
        return LevelManager::c_str(level);
    };

    std::ostream& operator<<(std::ostream &os, const myService::Level &level)
    {
        return os << LevelManager::toString(level);
    };

    std::istream& operator>>(std::istream &is, myService::Level &level)
    {
        std::string buf;
        is >> buf;
        level = LevelManager::toEnum(buf);
        return is;
    };

}
/* namespace ::myService */
